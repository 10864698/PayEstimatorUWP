<Page
    x:Class="PayEstimatorUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PayEstimatorUWP"
    xmlns:data="using:PayEstimatorUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <local:DoubleToStringConverter x:Key="DoubleToStringConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <ListView x:Name="lvGigs">

            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid  Background="Gray">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2.5*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Date"/>
                        <TextBlock Grid.Column="1" Text="Start"/>
                        <TextBlock Grid.Column="2" Text="End"/>
                        <TextBlock Grid.Column="3" Text="Client"/>
                        <TextBlock Grid.Column="4" Text="Venue"/>
                        <TextBlock Grid.Column="5" Text="Hours"/>
                        <TextBlock Grid.Column="6" Text="Skill"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2.5*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding StartDate}"/>
                        <TextBlock Grid.Column="1" Text="{Binding StartTime}"/>
                        <TextBlock Grid.Column="2" Text="{Binding EndTime}"/>
                        <TextBlock Grid.Column="3" Text="{Binding ClientName}"/>
                        <TextBlock Grid.Column="4" Text="{Binding VenueName}"/>
                        <TextBlock Grid.Column="5" Text="{Binding Hours}"/>
                        <TextBlock Grid.Column="6" Text="{Binding Skill}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

        <TextBlock Grid.Row="1" x:Name="tbHours">
      <Run Text="Total Hours: "/>
      <Run Text="{Binding Path=TotalHours, Converter={StaticResource DoubleToStringConverter}}"/>
        </TextBlock>

        <TextBlock Grid.Row="2" x:Name="tbGross">
      <Run Text="Gross Amount: "/>
      <Run Text="{Binding Path=GrossAmount, Converter={StaticResource DoubleToStringConverter}}"/>
        </TextBlock>

        <TextBlock Grid.Row="3" x:Name="tbTest">
      <Run Text="Test Amount: "/>
      <Run Text="{Binding Path=TotalTest, Converter={StaticResource DoubleToStringConverter}}"/>
        </TextBlock>
    </Grid>
</Page>
